<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanyuan.mapper.CutItemRefsmallitemConfigMapper">
	<!--mybatis ehcache缓存配置 -->
	<!-- 以下两个<cache>标签二选一,第一个可以输出日志,第二个不输出日志 <cache type="org.mybatis.caches.ehcache.LoggingEhcache"
/> -->
	<!-- <cache type="org.mybatis.caches.ehcache.EhcacheCache"/> -->
	<!-- 以下与实体类的中字段一致 -->
	<sql id="selectId">
	   ci.id,
       ci.name,
       ci.ref_check_big_item_id,
       ci.ref_check_small_item_id,
       ci.remark,
       ci.order_by
	</sql>

	<select id="findEnterprisePage" resultType="com.lanyuan.entity.CutItemRefsmallitemConfigFormMap">
		select
		<include refid="selectId" />
		FROM cut_items ci
		where 1 = 1
		<if test="name != null">
			AND ci.name = '${name}'
		</if>
		<if test="order_by != null">
			ORDER BY ${order_by}
		</if>
	</select>



</mapper>