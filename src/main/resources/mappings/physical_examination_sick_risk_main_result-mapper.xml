<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanyuan.mapper.PhysicalExaminationSickRiskMainResultMapper">
	<!--mybatis ehcache缓存配置 -->
	<!-- 以下两个<cache>标签二选一,第一个可以输出日志,第二个不输出日志 <cache type="org.mybatis.caches.ehcache.LoggingEhcache"
		/> -->
	<!-- <cache type="org.mybatis.caches.ehcache.EhcacheCache"/> -->
	<!-- 以下与实体类的中字段一致 -->
	<sql id="selectId">
  cbe.id,
  cbe.custom_id,
  cbe.organization_id,
  cbe.insert_time,
  cbe.isdelete,
  cbe.last_check_time,
  ci.idcard,
  ci.vipcard,
  ci.mobile,
  ci.username,
  ci.password,
  ci.name,
  ci.sex,
  ci.birthday,
  ci.body_height,
  ci.weight,
  ci.zhiwen_code,
  ci.zhiwen_path,
  lo.name AS organization_name
	</sql>




	<select id="findAllShowInReportSickRiskList" resultType="com.lanyuan.entity.PhysicalExaminationSickRiskMainResultFormMap">
SELECT
  pesrmr.id,
  pesrmr.examination_record_id,
  pesrmr.sick_risk_item_id,
  pesrmr.bmi_risk_rout,
  pesrmr.age_risk_rout,
  pesrmr.check_item_rout,
  pesrmr.total_rout,
  pesrmr.risk_leve,
  sri.name
FROM physical_examination_sick_risk_main_result pesrmr
  LEFT JOIN sick_risk_item sri
    ON pesrmr.sick_risk_item_id = sri.id
WHERE pesrmr.examination_record_id = ${examination_record_id} ORDER BY pesrmr.sick_risk_item_id ASC
	</select>



</mapper>