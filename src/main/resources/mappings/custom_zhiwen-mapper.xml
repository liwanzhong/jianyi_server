<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanyuan.mapper.CustomZhiwenMapper">
	<!--mybatis ehcache缓存配置 -->
	<!-- 以下两个<cache>标签二选一,第一个可以输出日志,第二个不输出日志 <cache type="org.mybatis.caches.ehcache.LoggingEhcache"
		/> -->
	<!-- <cache type="org.mybatis.caches.ehcache.EhcacheCache"/> -->
	<!-- 以下与实体类的中字段一致 -->

	<select id="findList" resultType="com.lanyuan.entity.CustomZhiwenFormMap">
SELECT
  cz.id,
  cz.custom_id,
  cz.zhiwen_code,
  cz.zhiwen_path,
  cz.insert_time,
  cz.finger_position,
  cz.Instrument_id
FROM custom_zhiwen cz
WHERE cz.custom_id = ${custom_id}
	</select>

</mapper>