<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanyuan.mapper.EquipmentMapper">
	<!--mybatis ehcache缓存配置 -->
	<!-- 以下两个<cache>标签二选一,第一个可以输出日志,第二个不输出日志 <cache type="org.mybatis.caches.ehcache.LoggingEhcache" 
		/> -->
	<!-- <cache type="org.mybatis.caches.ehcache.EhcacheCache"/> -->
	<!-- 以下与实体类的中字段一致 -->
	<sql id="selectId">
		instrument_info.id, instrument_info.istmt_code, instrument_info.ent_id, instrument_info.sub_ent_point_id, instrument_info.name, instrument_info.modle_id, instrument_info.remark,enterprise_info.name as entname,sub_enterprise_point.name as subentname
	</sql>

	<select id="findEnterprisePage" resultType="com.lanyuan.entity.EquipmentFormMap">
		select
		<include refid="selectId" />
		FROM instrument_info
		LEFT JOIN enterprise_info
		ON enterprise_info.id = instrument_info.ent_id
		LEFT JOIN sub_enterprise_point
		ON instrument_info.sub_ent_point_id = sub_enterprise_point.id
		where 1 = 1
		<if test="orderby != null">
			${orderby}
		</if>
	</select>

	<select id="findLasyOrderItemCout" resultType="java.lang.Integer">
		SELECT  order_by FROM  check_small_item ORDER BY order_by DESC limit 1;
	</select>
	
</mapper>